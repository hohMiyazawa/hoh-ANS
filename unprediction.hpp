#ifndef UNPREDICTION_HEADER
#define UNPREDICTION_HEADER

uint16_t* unpredict_all(
	uint16_t* data,
	size_t size,
	int width,
	int height,
	int depth,
	int x_tiles,
	int y_tiles,
	uint16_t* tile_map
){
	return data;
}

#endif //UNPREDICTION_HEADER
